[project]
name = "multi-crawler"
version = "0.1.0"
description = "多爬虫项目 - 共享依赖，独立运行"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.0",
    "beautifulsoup4>=4.12.0",
]

[tool.uv]
cache-dir = ".uv/cache"
